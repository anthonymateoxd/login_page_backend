pipeline {
    agent any

    environment {
        KUBECONFIG = 'C:\\ProgramData\\Jenkins\\.kube\\config'
    }


    stages {
        stage('Eliminar recursos Backend de Kubernetes') {
            steps {
                script {
                    echo "🔻 Eliminando recursos Backend del clúster..."
                    bat 'kubectl delete -f backend.yaml --ignore-not-found'
                    bat 'kubectl delete -f backend-service.yaml --ignore-not-found'
                }
            }
        }
    }

    post {
        success {
            echo "✅ Recursos eliminados correctamente del clúster."
        }
        failure {
            echo "❌ Ocurrió un error al eliminar los recursos. Revisa los logs."
        }
    }
}
